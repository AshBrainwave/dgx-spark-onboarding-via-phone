export function Connecting({ transport, onContinue }: { transport: "BLE" | "portal"; onContinue?: () => void }) {
  return <section><h2>Connecting to Spark</h2><ol><li>Checking the QR device key</li><li>{transport === "BLE" ? "Opening Bluetooth secure channel" : "Opening secure portal channel"}</li><li>Loading available Wi-Fi networks</li></ol>{onContinue && <button onClick={onContinue}>{transport === "BLE" ? "Choose your DGX Spark" : "Connect"}</button>}{transport === "BLE" && <p className="muted">Android will show its own Bluetooth chooser. Select the advertised DGX Spark device.</p>}</section>;
}
