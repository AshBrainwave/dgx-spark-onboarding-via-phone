import { useState } from "react";
export function Success({ ip, hostname, onName }: { ip: string; hostname: string; onName: (name: string) => void }) { const [name, setName] = useState("DGX Spark"); const command = `ssh ubuntu@${ip}`; return <section><h2>Spark is online</h2><label>Device name<input value={name} onChange={e => setName(e.target.value)} onBlur={() => onName(name)} /></label><p>LAN IP: <strong>{ip}</strong></p><code>{command}</code><button className="secondary" onClick={() => navigator.clipboard.writeText(command)}>Copy SSH command</button><a className="button" href={`http://${hostname}.local`}>Open Spark web UI</a></section>; }
