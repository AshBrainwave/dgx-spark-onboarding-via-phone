{
  "name": "spark-onboarding",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "build:portal": "tsc -b && vite build --mode portal && node scripts/inline-portal.mjs",
    "typecheck": "tsc -b",
    "lint": "eslint src",
    "test": "vitest"
  },
  "dependencies": {
    "@noble/ciphers": "^2.2.0",
    "@noble/curves": "^2.2.0",
    "@noble/hashes": "^2.2.0",
    "@vitejs/plugin-react": "latest",
    "@zxing/browser": "^0.2.1",
    "react": "latest",
    "react-dom": "latest",
    "typescript": "latest",
    "vite": "latest",
    "zod": "latest"
  },
  "devDependencies": {
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "eslint": "latest",
    "vitest": "latest"
  }
}
